version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
  frontend:
    build: .
    command: streamlit run ui/dashboard.py
    ports:
      - "8501:8501"
    volumes:
      - .:/app